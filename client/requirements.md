## Packages
date-fns | Formatting dates and times for the clock and tasks
framer-motion | Smooth animations for messages and tasks
lucide-react | Beautiful icons for the UI

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
